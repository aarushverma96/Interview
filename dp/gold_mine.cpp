#include <bits/stdc++.h>

using namespace std;

int main()
{
  return 0;
}

int maxGold(std::vector<int> gold[],int row)
{
  int g[col+1][gold[0].size()];
  memset(g,0,sizeof(g));

  for (int i = gold[0].size(); i>=0; i--)
  {
    for (int j = 0; j <=col ; j++)
    {

    }
  }


}
s
